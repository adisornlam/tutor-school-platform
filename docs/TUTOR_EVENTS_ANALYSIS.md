# 📅 Tutor Events System Analysis

> ⚠️ **Deprecated**: โปรดใช้ [CALENDAR_EVENTS_ANALYSIS.md](./CALENDAR_EVENTS_ANALYSIS.md) แทน
> 
> เอกสารนี้เป็น version เดิมที่ออกแบบเฉพาะสำหรับ Tutor เท่านั้น
> Version ใหม่เป็นระบบ Calendar กลางๆ ที่ทุก role ใช้ได้

## 🎯 ภาพรวม

ระบบ Event สำหรับครูเพื่อเพิ่มกิจกรรมส่วนตัวลงในปฏิทิน และสามารถแชร์ให้คนอื่นเห็นได้

---

## 📊 Database Schema

### ตาราง: `tutor_events`

```sql
CREATE TABLE IF NOT EXISTS tutor_events (
    id INT PRIMARY KEY AUTO_INCREMENT,
    tutor_id INT NOT NULL,
    title VARCHAR(300) NOT NULL,
    description TEXT,
    start_datetime DATETIME NOT NULL,
    end_datetime DATETIME NOT NULL,
    location VARCHAR(500),
    color VARCHAR(7) DEFAULT '#3B82F6', -- Hex color code
    is_all_day BOOLEAN DEFAULT FALSE,
    reminder_minutes INT DEFAULT NULL, -- 15, 30, 60, etc. (NULL = no reminder)
    is_shared BOOLEAN DEFAULT FALSE,
    shared_scope ENUM('private', 'tutors', 'admins', 'branch_admins', 'public') DEFAULT 'private',
    shared_branch_id INT NULL, -- ถ้าแชร์แค่ branch ใด branch หนึ่ง
    created_by INT NOT NULL, -- user_id
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (tutor_id) REFERENCES tutors(id) ON DELETE CASCADE,
    FOREIGN KEY (shared_branch_id) REFERENCES branches(id) ON DELETE SET NULL,
    FOREIGN KEY (created_by) REFERENCES users(id),
    INDEX idx_tutor_datetime (tutor_id, start_datetime),
    INDEX idx_shared_scope (is_shared, shared_scope),
    INDEX idx_datetime (start_datetime)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### ตาราง: `tutor_event_shared_with` (Optional - สำหรับแชร์แบบระบุคน)

```sql
CREATE TABLE IF NOT EXISTS tutor_event_shared_with (
    id INT PRIMARY KEY AUTO_INCREMENT,
    event_id INT NOT NULL,
    shared_with_user_id INT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (event_id) REFERENCES tutor_events(id) ON DELETE CASCADE,
    FOREIGN KEY (shared_with_user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE KEY unique_event_user (event_id, shared_with_user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 🎨 Features ที่ควรมี

### 1. **สร้าง Event**
- ✅ ชื่อ Event (title) - **required**
- ✅ คำอธิบาย (description) - optional
- ✅ วันเวลาเริ่มต้น (start_datetime) - **required**
- ✅ วันเวลาสิ้นสุด (end_datetime) - **required**
- ✅ ทั้งวัน (all day event) - checkbox
- ✅ สถานที่ (location) - optional
- ✅ สี Event (color picker) - default blue
- ✅ ตั้งเตือน (reminder) - dropdown (ไม่มี, 15 นาที, 30 นาที, 1 ชั่วโมง, 1 วัน)
- ✅ แชร์ Event (checkbox)
  - ถ้าเลือกแชร์ → แสดงตัวเลือกการแชร์

### 2. **ตัวเลือกการแชร์ (Share Options)**

เมื่อติ๊ก checkbox "แชร์" แล้ว จะมีตัวเลือก:

#### A. **แชร์แบบ Public (ทุกคนเห็น)**
- ✅ `shared_scope = 'public'`
- ทุกคนในระบบเห็น event นี้

#### B. **แชร์ให้ Tutors (ครูทุกคน)**
- ✅ `shared_scope = 'tutors'`
- ครูทุกคนเห็น event นี้

#### C. **แชร์ให้ Admins (ผู้ดูแลระบบ)**
- ✅ `shared_scope = 'admins'`
- System Admin, Owner, Admin กลาง เห็น event นี้

#### D. **แชร์ให้ Branch Admins (ผู้ดูแลสาขา)**
- ✅ `shared_scope = 'branch_admins'`
- Branch Admin ทุกคนเห็น event นี้
- หรือเลือกสาขาเฉพาะ → `shared_branch_id`

#### E. **แชร์ให้เฉพาะคน (Advanced - Optional)**
- ✅ `shared_scope = 'private'` + ใช้ตาราง `tutor_event_shared_with`
- เลือกผู้ใช้ที่ต้องการแชร์เป็นรายบุคคล

### 3. **แก้ไข Event**
- แก้ไขข้อมูลทั้งหมดได้เหมือนตอนสร้าง
- เปลี่ยนสถานะการแชร์ได้

### 4. **ลบ Event**
- ลบได้เฉพาะ event ที่ตัวเองสร้าง
- ถ้ามีการแชร์ → คนอื่นจะไม่เห็น event นี้แล้ว

### 5. **ดู Event ในปฏิทิน**
- แสดง event ส่วนตัวด้วยสีที่กำหนด
- แสดง event ที่แชร์มาแยกสี (เช่น สีเทา)
- ในปฏิทินสามารถคลิกเพื่อดูรายละเอียดได้

---

## 🔌 API Endpoints

### GET `/api/admin/tutor/events`
**Description**: ดึง events ของ tutor (รวมทั้งที่แชร์มา)

**Query Params**:
- `start_date`: วันที่เริ่มต้น (filter)
- `end_date`: วันที่สิ้นสุด (filter)
- `include_shared`: boolean (default: true) - รวม events ที่แชร์มา

**Response**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "tutor_id": 5,
      "title": "ประชุมทีม",
      "description": "ประชุมวางแผนการสอน",
      "start_datetime": "2025-01-25T10:00:00",
      "end_datetime": "2025-01-25T11:00:00",
      "location": "ห้องประชุม",
      "color": "#10B981",
      "is_all_day": false,
      "reminder_minutes": 15,
      "is_shared": true,
      "shared_scope": "tutors",
      "shared_branch_id": null,
      "is_mine": true, // true = สร้างเอง, false = แชร์มา
      "created_by": {
        "id": 10,
        "first_name": "สมชาย",
        "last_name": "ใจดี"
      }
    }
  ]
}
```

### POST `/api/admin/tutor/events`
**Description**: สร้าง event ใหม่

**Request Body**:
```json
{
  "title": "ประชุมทีม",
  "description": "ประชุมวางแผนการสอน",
  "start_datetime": "2025-01-25T10:00:00",
  "end_datetime": "2025-01-25T11:00:00",
  "location": "ห้องประชุม",
  "color": "#10B981",
  "is_all_day": false,
  "reminder_minutes": 15,
  "is_shared": true,
  "shared_scope": "tutors",
  "shared_branch_id": null
}
```

**Response**:
```json
{
  "success": true,
  "data": {
    "id": 1,
    // ... event data
  }
}
```

### PUT `/api/admin/tutor/events/:id`
**Description**: แก้ไข event

**Access**: เจ้าของ event เท่านั้น

**Request Body**: เหมือน POST

### DELETE `/api/admin/tutor/events/:id`
**Description**: ลบ event

**Access**: เจ้าของ event เท่านั้น

### GET `/api/admin/tutor/events/:id`
**Description**: ดูรายละเอียด event

---

## 🎨 UI Components

### 1. **Event Modal (Create/Edit)**

```
┌─────────────────────────────────────┐
│ สร้าง Event              [X]        │
├─────────────────────────────────────┤
│ ชื่อ Event *                        │
│ [_____________________________]     │
│                                     │
│ คำอธิบาย                            │
│ [_____________________________]     │
│ [_____________________________]     │
│                                     │
│ ┌──────────┐  ┌──────────┐         │
│ │ เริ่มต้น*│  │ สิ้นสุด* │         │
│ │[วันที่/เวลา]│ │[วันที่/เวลา]│         │
│ └──────────┘  └──────────┘         │
│                                     │
│ ☑ ทั้งวัน                            │
│                                     │
│ สถานที่                             │
│ [_____________________________]     │
│                                     │
│ สี Event                            │
│ [🔵] [🟢] [🟡] [🟠] [🔴] [🟣]     │
│                                     │
│ ตั้งเตือน                           │
│ [เลือก: ไม่มี / 15 นาที / ...]     │
│                                     │
│ ☑ แชร์ Event                        │
│                                     │
│ (ถ้าเลือกแชร์)                      │
│ แชร์ให้:                            │
│ ○ ทุกคน                             │
│ ○ ครูทุกคน                           │
│ ○ ผู้ดูแลระบบ                        │
│ ○ ผู้ดูแลสาขา                        │
│   └─ [เลือกสาขา: ______]            │
│                                     │
│ ┌──────────┐  ┌──────────┐         │
│ │  ยกเลิก  │  │   บันทึก  │         │
│ └──────────┘  └──────────┘         │
└─────────────────────────────────────┘
```

### 2. **Calendar Integration**

- Event ส่วนตัวแสดงด้วยสีที่เลือก
- Event ที่แชร์มาแสดงด้วยสีเทา/สีที่แตกต่าง
- Hover แสดง tooltip: "สร้างโดย: [ชื่อ]", "แชร์โดย: [ชื่อ]"
- คลิก event เพื่อดู/แก้ไข/ลบ

### 3. **Event List View**

แสดงรายการ events พร้อม filter:
- แสดงของฉันเท่านั้น / รวมที่แชร์มา
- Filter ตามวันที่
- แยกสีระหว่าง event ส่วนตัว vs แชร์มา

---

## 🔐 Security & Permissions

1. **สร้าง Event**: Tutor เท่านั้น
2. **แก้ไข/ลบ Event**: เจ้าของ event เท่านั้น
3. **ดู Event**:
   - Event ส่วนตัว: เจ้าของเท่านั้น
   - Event ที่แชร์: ตาม `shared_scope` ที่กำหนด
4. **Admin/System Admin**: ดู events ทั้งหมดได้ (ถ้าจำเป็น)

---

## 📝 Business Rules

1. **Event ที่แชร์**:
   - เจ้าของแก้ไข/ลบได้ → คนอื่นจะไม่เห็นแล้ว
   - ถ้าแก้ไขข้อมูล → คนอื่นจะเห็นการเปลี่ยนแปลง

2. **Event Conflict**:
   - ไม่ต้องเช็ค conflict กับ course_schedules (เป็น event ส่วนตัว)
   - แต่ถ้าต้องการ อาจจะแสดง warning ถ้ามี overlap

3. **Reminder**:
   - ใช้ browser notification หรือ email (optional)
   - หรือแสดงใน dashboard

4. **Color Coding**:
   - Event ส่วนตัว: ใช้สีที่เลือก
   - Event ที่แชร์มา: สีเทา (#6B7280) หรือสี default
   - Course Schedule: สีอื่น (เช่น สีเขียว)

---

## 🚀 Implementation Priority

### Phase 1 (Core Features):
1. ✅ Database schema
2. ✅ API endpoints (CRUD)
3. ✅ Event Modal (Create/Edit)
4. ✅ แสดง Event ในปฏิทิน
5. ✅ การแชร์แบบพื้นฐาน (tutors, admins, public)

### Phase 2 (Advanced):
1. ⏳ การแชร์แบบระบุคน (tutor_event_shared_with)
2. ⏳ Reminder notifications
3. ⏳ Event conflict detection
4. ⏳ Recurring events (optional)
5. ⏳ Export to calendar (iCal format)

---

## 💡 Additional Considerations

1. **Event Types**: อาจจะแบ่งเป็น
   - Personal (ส่วนตัว)
   - Meeting (ประชุม)
   - Holiday (วันหยุด)
   - Other (อื่นๆ)

2. **Recurring Events**: สำหรับ event ที่เกิดขึ้นซ้ำๆ เช่น "ประชุมทุกวันจันทร์"

3. **Event Attachments**: เพิ่มไฟล์แนบ (optional)

4. **Event Comments**: ให้คนที่เห็น event แสดงความคิดเห็นได้ (optional)

---

## 📊 Example Use Cases

### Use Case 1: ครูสร้าง Event ส่วนตัว
- ครู A สร้าง event "วันหยุดส่วนตัว" 
- `is_shared = false`
- แสดงเฉพาะในปฏิทินของครู A

### Use Case 2: ครูแชร์ Event ให้ครูทุกคน
- ครู A สร้าง event "ประชุมทีมครู"
- `is_shared = true`, `shared_scope = 'tutors'`
- ครูทุกคนเห็น event นี้ในปฏิทิน

### Use Case 3: ครูแชร์ Event ให้ Branch Admin
- ครู A สร้าง event "ประชุมกับ Branch Admin"
- `is_shared = true`, `shared_scope = 'branch_admins'`, `shared_branch_id = 1`
- Branch Admin ของสาขา 1 เห็น event นี้

### Use Case 4: ครูดู Event ที่แชร์มา
- ครู B เข้าดูปฏิทิน
- เห็นทั้ง event ส่วนตัวและ event ที่ครู A แชร์มา
- แยกสีได้ชัดเจน

