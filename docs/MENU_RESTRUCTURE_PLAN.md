# 📋 Menu Restructure Plan

**วันที่**: 2025-01-19

---

## 🎯 วัตถุประสงค์

ปรับโครงสร้างเมนูให้แยกผู้ใช้บริการ (นักเรียน/ผู้ปกครอง) ออกจากผู้ให้บริการ (อาจารย์/Admin) ให้ชัดเจน

---

## 📊 โครงสร้างเมนูใหม่

### **Level 1: เมนูหลัก**

1. **Dashboard** (`/admin`)
   - สรุปภาพรวม

2. **ผู้เรียน** (`/admin/students`) ⭐ เปลี่ยนจาก "จัดการผู้ใช้งาน"
   - แสดงเฉพาะนักเรียน
   - เมื่อดูข้อมูลนักเรียน จะเห็นข้อมูลผู้ปกครองด้วย
   - Features:
     - ดูรายการนักเรียน
     - ดูข้อมูลผู้ปกครองของนักเรียน
     - แก้ไขข้อมูลนักเรียน
     - ปิด/เปิดใช้งาน
     - ลบ

3. **คอร์สเรียน** (`/admin/courses`)
   - จัดการคอร์สเรียน

4. **การลงทะเบียน** (`/admin/enrollments`)
   - จัดการการลงทะเบียน

5. **การชำระเงิน** (`/admin/payments`)
   - จัดการการชำระเงิน

6. **โปรโมชั่น** (`/admin/promotions`)
   - จัดการโปรโมชั่น

7. **รายงาน** (Parent - มี submenu)
   - รายงานผู้ใช้งาน
   - รายงานคอร์สเรียน
   - รายงานการลงทะเบียน
   - รายงานการชำระเงิน
   - รายงานรายได้

8. **ตั้งค่า** (Parent - มี submenu)
   - ตั้งค่าอีเมล์
   - ตั้งค่าระบบ
   - สาขา
   - ระดับชั้น
   - วิชา
   - สิ่งที่ได้รับ
   - บทบาทผู้ใช้
   - วิธีการชำระเงิน
   - **จัดการผู้ใช้งาน** ⭐ เพิ่มใหม่
     - แสดงเป็น Tab:
       - อาจารย์
       - Admin สาขา

---

## 🔄 การเปลี่ยนแปลง

### **1. เมนู "จัดการผู้ใช้งาน" → "ผู้เรียน"**

**เดิม:**
- เมนู: "จัดการผู้ใช้งาน" (`/admin/users`)
- แสดง: ทั้งหมด, อาจารย์, นักเรียน, ผู้ปกครอง, Admin สาขา

**ใหม่:**
- เมนู: "ผู้เรียน" (`/admin/students`)
- แสดง: เฉพาะนักเรียน
- เมื่อดูข้อมูลนักเรียน → แสดงข้อมูลผู้ปกครองด้วย

### **2. เพิ่มเมนู "จัดการผู้ใช้งาน" ใน "ตั้งค่า"**

**ใหม่:**
- เมนู: "จัดการผู้ใช้งาน" (`/admin/settings/users`)
- แสดงเป็น Tab:
  - Tab 1: อาจารย์
  - Tab 2: Admin สาขา
- Features:
  - เพิ่ม, แก้ไข, ปิด/เปิด, ลบ

---

## 💡 ข้อดี

1. **แยกชัดเจน**: ผู้ใช้บริการ (นักเรียน/ผู้ปกครอง) vs ผู้ให้บริการ (อาจารย์/Admin)
2. **ง่ายต่อการใช้งาน**: Admin รู้ว่าต้องไปที่ไหนจัดการอะไร
3. **เป็นระเบียบ**: ผู้เรียนอยู่ในเมนูหลัก, ผู้ให้บริการอยู่ในตั้งค่า

---

## 📝 Implementation Plan

### **1. อัปเดต Menu Migration Script**
- เปลี่ยน `USERS` → `STUDENTS`
- เพิ่ม `SETTINGS_USERS` ใน `SETTINGS`

### **2. สร้าง/แก้ไข API Endpoints**
- `GET /api/admin/students` - ดูรายการนักเรียน (พร้อมผู้ปกครอง)
- `GET /api/admin/students/:id` - ดูข้อมูลนักเรียน + ผู้ปกครอง
- `GET /api/admin/settings/users` - ดูรายการอาจารย์ + Admin สาขา (แยกเป็น tab)

### **3. สร้าง Frontend Pages**
- `/admin/students/index.vue` - หน้าจัดการนักเรียน
- `/admin/settings/users/index.vue` - หน้าจัดการผู้ให้บริการ (Tab: อาจารย์, Admin สาขา)

---

*Menu Restructure Plan - Last Updated: 2025-01-19*

