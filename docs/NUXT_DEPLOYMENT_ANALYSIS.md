# วิเคราะห์ Nuxt Deployment Documentation

**Source**: [Nuxt 4 Deployment Guide](https://nuxt.com/docs/4.x/getting-started/deployment)

---

## 📋 สรุปเนื้อหาที่มีประโยชน์

### 1. **Node.js Server Deployment**

**Entry Point:**
```bash
node .output/server/index.mjs
```

**Environment Variables:**
- `NITRO_PORT` or `PORT` (defaults to `3000`)
- `NITRO_HOST` or `HOST` (defaults to `'0.0.0.0'`)
- `NITRO_SSL_CERT` and `NITRO_SSL_KEY` (for HTTPS)

**ประโยชน์สำหรับโปรเจคนี้:**
- ✅ รู้ว่า entry point คือ `.output/server/index.mjs`
- ✅ รู้ว่าใช้ `PORT` environment variable
- ✅ รู้ว่า default port คือ `3000` (แต่โปรเจคนี้ใช้ `4000`)

### 2. **PM2 Support**

**ecosystem.config.cjs:**
```javascript
module.exports = {
  apps: [
    {
      name: 'NuxtAppName',
      port: '3000',
      exec_mode: 'cluster',
      instances: 'max',
      script: './.output/server/index.mjs',
    },
  ],
}
```

**ประโยชน์สำหรับโปรเจคนี้:**
- ⚠️ ไม่ได้ใช้ PM2 (ใช้ Passenger/cPanel)
- ⚠️ แต่สามารถใช้เป็น reference สำหรับ future deployment

### 3. **Cluster Mode**

**NITRO_PRESET=node_cluster:**
- ใช้ Node.js cluster module
- Round robin strategy

**ประโยชน์สำหรับโปรเจคนี้:**
- ⚠️ ไม่ได้ใช้ cluster mode (ใช้ Passenger/cPanel)
- ⚠️ แต่สามารถใช้เป็น reference

### 4. **Presets**

**Configuration:**
```typescript
export default defineNuxtConfig({
  nitro: {
    preset: 'node-server',
  },
})
```

**ประโยชน์สำหรับโปรเจคนี้:**
- ✅ รู้ว่าใช้ `node-server` preset (default)
- ✅ รู้ว่าสามารถเปลี่ยน preset ได้

---

## 🔍 วิเคราะห์ความเกี่ยวข้องกับโปรเจคนี้

### ✅ **มีประโยชน์**

1. **Entry Point**
   - ✅ รู้ว่า entry point คือ `.output/server/index.mjs`
   - ✅ ตรงกับที่ใช้ใน cPanel Passenger

2. **Environment Variables**
   - ✅ รู้ว่าใช้ `PORT` environment variable
   - ✅ ตรงกับที่ตั้งค่าใน cPanel

3. **Preset**
   - ✅ รู้ว่าใช้ `node-server` preset (default)
   - ✅ ไม่ต้องตั้งค่าเพิ่มเติม

### ⚠️ **ไม่เกี่ยวข้องโดยตรง**

1. **PM2**
   - ⚠️ ไม่ได้ใช้ PM2 (ใช้ Passenger/cPanel)
   - ⚠️ แต่สามารถใช้เป็น reference

2. **Cluster Mode**
   - ⚠️ ไม่ได้ใช้ cluster mode
   - ⚠️ Passenger จัดการ process management

3. **Static Hosting**
   - ⚠️ ไม่ได้ใช้ static hosting (ใช้ SSR)
   - ⚠️ ไม่เกี่ยวข้อง

---

## 💡 สรุป

### **มีประโยชน์สำหรับโปรเจคนี้:**

1. ✅ **Entry Point** - รู้ว่าใช้ `.output/server/index.mjs`
2. ✅ **Environment Variables** - รู้ว่าใช้ `PORT` environment variable
3. ✅ **Preset** - รู้ว่าใช้ `node-server` preset

### **ไม่เกี่ยวข้องโดยตรง:**

1. ⚠️ **PM2** - ไม่ได้ใช้ (ใช้ Passenger/cPanel)
2. ⚠️ **Cluster Mode** - ไม่ได้ใช้
3. ⚠️ **Static Hosting** - ไม่ได้ใช้ (ใช้ SSR)

---

## 🎯 คำแนะนำ

### **สำหรับโปรเจคนี้:**

1. ✅ **ใช้ Entry Point**: `.output/server/index.mjs` (ใช้อยู่แล้ว)
2. ✅ **ใช้ Environment Variables**: `PORT` (ตั้งค่าแล้ว)
3. ✅ **ใช้ node-server preset**: Default (ไม่ต้องตั้งค่า)

### **ไม่ต้องทำอะไรเพิ่ม:**

- ⚠️ ไม่ต้องใช้ PM2 (ใช้ Passenger/cPanel)
- ⚠️ ไม่ต้องใช้ cluster mode
- ⚠️ ไม่ต้องเปลี่ยน preset

---

**สรุป**: Nuxt deployment docs มีประโยชน์บ้าง แต่ไม่เกี่ยวข้องโดยตรงกับปัญหาที่เจอ (database connection bundling issues) ✅

