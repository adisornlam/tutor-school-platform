# 📋 วิเคราะห์ System Settings

## ✅ สถานะปัจจุบัน

### ตาราง Database
❌ **ยังไม่มีตารางสำหรับเก็บ System Settings**

ปัจจุบันการตั้งค่าระบบถูกเก็บใน:
- Environment variables (`.env`)
- `nuxt.config.ts` (runtimeConfig)
- Hard-coded ในโค้ด

---

## 🎯 ฟีเจอร์ที่ควรมีใน System Settings

### 1. **ข้อมูลทั่วไป (General Information)**
- **ชื่อแอปพลิเคชัน (Application Name)**
  - ตัวอย่าง: "KDC Tutor School"
  - ใช้แสดงใน header, title, email templates
  
- **เวอร์ชัน (Version)**
  - ตัวอย่าง: "1.0.0"
  - ใช้แสดงใน footer, about page
  
- **โลโก้/ไอคอน (Logo/Icon)**
  - URL โลโก้สำหรับแสดงในหน้าเว็บ
  - Favicon URL

---

### 2. **การตั้งค่าเวลา (Timezone & Date/Time)**
- **Timezone**
  - ตัวอย่าง: "Asia/Bangkok" (+07:00)
  - ใช้สำหรับแสดงเวลาในระบบ
  
- **รูปแบบวันที่ (Date Format)**
  - ตัวอย่าง: "DD/MM/YYYY", "YYYY-MM-DD"
  
- **รูปแบบเวลา (Time Format)**
  - ตัวอย่าง: "24-hour", "12-hour"

---

### 3. **การตั้งค่าการบำรุงรักษา (Maintenance Mode)**
- **เปิด/ปิด Maintenance Mode**
  - เมื่อเปิด: แสดงหน้า maintenance page แทนหน้าเว็บปกติ
  - Message สำหรับผู้ใช้
  
- **กำหนดเวลาบำรุงรักษา (Scheduled Maintenance)**
  - วันที่/เวลาเริ่มต้น
  - วันที่/เวลาสิ้นสุด
  
- **IP Whitelist**
  - IP ที่สามารถเข้าถึงได้ระหว่าง maintenance mode (สำหรับ admin)

---

### 4. **การตั้งค่าความปลอดภัย (Security Settings)**
- **Session Timeout**
  - ระยะเวลาหมดอายุของ session (นาที)
  - ตัวอย่าง: 30, 60, 120 นาที
  
- **Password Policy**
  - ความยาวขั้นต่ำ
  - ต้องมีตัวอักษรพิเศษหรือไม่
  - ต้องมีตัวพิมพ์ใหญ่/เล็กหรือไม่
  
- **Login Attempts Limit**
  - จำนวนครั้งที่พยายาม login ได้ก่อนถูก lock
  - ระยะเวลาที่ถูก lock (นาที)
  
- **Two-Factor Authentication (2FA)**
  - เปิด/ปิด 2FA สำหรับ admin
  - เปิด/ปิด 2FA สำหรับ users

---

### 5. **การตั้งค่าอีเมล (Email Settings)**
⚠️ **Note**: มีหน้า Email Settings แล้วที่ `/admin/settings/email`
- SMTP Configuration
- Email Templates

---

### 6. **การตั้งค่าไฟล์ (File Upload Settings)**
- **Max File Size**
  - ขนาดไฟล์สูงสุดที่อัพโหลดได้ (MB)
  - ตัวอย่าง: 10, 20, 50 MB
  
- **Allowed File Types**
  - ประเภทไฟล์ที่อนุญาตให้อัพโหลด
  - ตัวอย่าง: jpg, png, pdf, doc, docx
  
- **Storage Location**
  - Local storage
  - Cloud storage (S3, etc.)

---

### 7. **การตั้งค่าการแจ้งเตือน (Notification Settings)**
- **เปิด/ปิดการแจ้งเตือนแต่ละประเภท**
  - Email notifications
  - SMS notifications
  - Push notifications (ถ้ามี)
  
- **การแจ้งเตือนอัตโนมัติ**
  - แจ้งเตือนก่อนคอร์สเริ่ม (กี่นาที/ชั่วโมง)
  - แจ้งเตือนก่อนวันชำระเงิน

---

### 8. **การตั้งค่าการชำระเงิน (Payment Settings)**
⚠️ **Note**: มีเมนู Payment Methods แล้วที่ `/admin/settings/payment-methods`
- วิธีการชำระเงิน
- สถานะการชำระเงิน

---

### 9. **การตั้งค่าภาษา (Language/Locale Settings)**
- **ภาษาหลัก (Default Language)**
  - ตัวอย่าง: "th", "en"
  
- **ภาษาที่รองรับ (Supported Languages)**
  - รายการภาษาที่ระบบรองรับ
  - ตัวอย่าง: ["th", "en"]

---

### 10. **การตั้งค่าการแสดงผล (Display Settings)**
- **จำนวนรายการต่อหน้า (Items Per Page)**
  - ตัวอย่าง: 10, 20, 50, 100
  
- **Theme/Color Scheme**
  - Light/Dark mode
  - Primary color
  - Secondary color

---

### 11. **การตั้งค่า API & Integration**
- **API Rate Limiting**
  - จำนวน requests ต่อนาที/ชั่วโมง
  
- **Third-party Integrations**
  - Google OAuth (ถ้ามี)
  - Facebook OAuth (ถ้ามี)
  - LINE OAuth (ถ้ามี)
  - Payment Gateway (ถ้ามี)

---

### 12. **การตั้งค่าการสำรองข้อมูล (Backup Settings)**
- **Auto Backup**
  - เปิด/ปิด auto backup
  - ความถี่ในการ backup (daily, weekly, monthly)
  
- **Backup Retention**
  - เก็บ backup กี่วัน/เดือน

---

## 📊 โครงสร้าง Database ที่แนะนำ

### ตาราง `system_settings`

```sql
CREATE TABLE IF NOT EXISTS system_settings (
    id INT PRIMARY KEY AUTO_INCREMENT,
    key VARCHAR(100) UNIQUE NOT NULL,
    value TEXT,
    type ENUM('string', 'number', 'boolean', 'json') DEFAULT 'string',
    category VARCHAR(50) NOT NULL,
    description TEXT,
    is_public BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_key (key),
    INDEX idx_category (category)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**Categories**:
- `general` - ข้อมูลทั่วไป
- `timezone` - การตั้งค่าเวลา
- `maintenance` - Maintenance mode
- `security` - ความปลอดภัย
- `file_upload` - การอัพโหลดไฟล์
- `notification` - การแจ้งเตือน
- `language` - ภาษา
- `display` - การแสดงผล
- `api` - API settings
- `backup` - Backup settings

**ตัวอย่าง Data**:
```sql
INSERT INTO system_settings (key, value, type, category, description, is_public) VALUES
('app_name', 'KDC Tutor School', 'string', 'general', 'ชื่อแอปพลิเคชัน', TRUE),
('app_version', '1.0.0', 'string', 'general', 'เวอร์ชัน', TRUE),
('timezone', 'Asia/Bangkok', 'string', 'timezone', 'Timezone', FALSE),
('maintenance_mode', 'false', 'boolean', 'maintenance', 'เปิด/ปิด Maintenance Mode', FALSE),
('maintenance_message', 'ระบบกำลังบำรุงรักษา กรุณาลองใหม่อีกครั้ง', 'string', 'maintenance', 'ข้อความ Maintenance', FALSE),
('session_timeout', '120', 'number', 'security', 'Session timeout (นาที)', FALSE),
('max_file_size', '10', 'number', 'file_upload', 'ขนาดไฟล์สูงสุด (MB)', FALSE),
('items_per_page', '20', 'number', 'display', 'จำนวนรายการต่อหน้า', FALSE),
('default_language', 'th', 'string', 'language', 'ภาษาหลัก', TRUE);
```

---

## 🎨 UI/UX ที่แนะนำ

### Layout
- ใช้ **Tabs** หรือ **Sections** เพื่อจัดกลุ่มการตั้งค่า
- แต่ละ tab/section แสดงฟอร์มสำหรับการตั้งค่าในหมวดนั้น
- มีปุ่ม "บันทึก" สำหรับแต่ละ section หรือ "บันทึกทั้งหมด" สำหรับทั้งหน้า

### Features
- **Real-time Preview** (ถ้าเป็นไปได้)
- **Validation** ก่อนบันทึก
- **Confirmation** เมื่อเปลี่ยนค่าสำคัญ (เช่น maintenance mode)
- **History/Log** ของการเปลี่ยนแปลง (optional)

---

## 🔐 Security Considerations

1. **Access Control**: เฉพาะ `system_admin` และ `owner` เท่านั้นที่เข้าถึงได้
2. **Validation**: ตรวจสอบค่าที่บันทึกก่อนเก็บในฐานข้อมูล
3. **Sensitive Data**: ข้อมูลที่ sensitive (เช่น API keys) ควรเข้ารหัสก่อนเก็บ
4. **Audit Trail**: บันทึกว่าใครเปลี่ยนอะไรเมื่อไหร่ (optional แต่แนะนำ)

---

## 📝 Priority

### Priority 1 (Critical)
- ✅ General Information (app name, version)
- ✅ Timezone
- ✅ Maintenance Mode
- ✅ Security Settings (session timeout)

### Priority 2 (Important)
- ✅ File Upload Settings
- ✅ Notification Settings
- ✅ Language Settings
- ✅ Display Settings

### Priority 3 (Nice to have)
- ⚠️ API & Integration Settings
- ⚠️ Backup Settings
- ⚠️ Audit Trail

---

## 🚀 Implementation Steps

1. **สร้างตาราง `system_settings`**
2. **สร้าง API Endpoints**:
   - `GET /admin/settings/system` - ดึงการตั้งค่าทั้งหมด
   - `PUT /admin/settings/system` - อัปเดตการตั้งค่า
   - `GET /admin/settings/system/[key]` - ดึงการตั้งค่าเฉพาะ key
   - `PUT /admin/settings/system/[key]` - อัปเดตการตั้งค่าเฉพาะ key
3. **สร้าง UI Page** ที่ `/admin/settings/system`
4. **สร้าง Components** สำหรับแต่ละ section
5. **Integrate** กับระบบที่มีอยู่ (เช่น maintenance mode middleware)

---

*Last updated: 2025-01-20*

