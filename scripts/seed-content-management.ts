import mysql from 'mysql2/promise'
import { readFileSync } from 'fs'
import { join } from 'path'

const config = {
  host: process.env.DB_HOST || 'localhost',
  port: parseInt(process.env.DB_PORT || '3307'),
  user: process.env.DB_USER || 'root',
  password: process.env.DB_PASSWORD || '',
  database: process.env.DB_NAME || 'tutordb'
}

interface Article {
  title: string
  slug: string
  excerpt: string
  content: string
  category: string
  icon: string
  featured_image_url?: string
  author_id: number
  status: 'draft' | 'published' | 'archived'
  is_featured: boolean
  display_order: number
  published_at: Date | null
}

interface Testimonial {
  name: string
  role: string
  comment: string
  rating: number
  avatar_url?: string
  status: 'pending' | 'approved' | 'rejected'
  display_order: number
}

async function seedContentManagement() {
  const connection = await mysql.createConnection(config)
  
  try {
    console.log('📝 Seeding content management data...')
    console.log('')
    
    // Get admin user ID (for author_id)
    const [adminUsers] = await connection.execute(
      'SELECT id FROM users WHERE email = ? OR email = ?',
      ['admin@kdcschool.com', 'admincenter@kdcschool.com']
    ) as any[]
    
    let authorId = 1 // Default fallback
    if (adminUsers.length > 0) {
      authorId = adminUsers[0].id
    } else {
      // Get first active user
      const [users] = await connection.execute(
        'SELECT id FROM users WHERE status = "active" LIMIT 1'
      ) as any[]
      if (users.length > 0) {
        authorId = users[0].id
      }
    }
    
    console.log(`Using author ID: ${authorId}`)
    console.log('')
    
    // ============================================
    // ARTICLES
    // ============================================
    console.log('📄 Creating sample articles...')
    
    const articles: Article[] = [
      {
        title: '10 เทคนิคเตรียมสอบให้ได้คะแนนดี',
        slug: '10-techniques-prepare-exam-good-score',
        excerpt: 'เรียนรู้เทคนิคการเตรียมสอบที่มีประสิทธิภาพ จากประสบการณ์ของติวเตอร์มืออาชีพ เพื่อให้ได้คะแนนตามที่ตั้งเป้าหมายไว้',
        content: `<h2>เทคนิคการเตรียมสอบให้ได้คะแนนดี</h2>
<p>การเตรียมสอบอย่างมีประสิทธิภาพเป็นสิ่งสำคัญที่ช่วยให้นักเรียนสามารถทำคะแนนได้ตามที่ตั้งเป้าหมายไว้ วันนี้เรามีเทคนิคดีๆ มาแนะนำค่ะ</p>

<h3>1. วางแผนการเรียนล่วงหน้า</h3>
<p>เริ่มต้นด้วยการวางแผนการเรียนล่วงหน้าโดยกำหนดตารางเวลาการอ่านหนังสือและทำแบบฝึกหัดให้เหมาะสมกับแต่ละวิชา</p>

<h3>2. สรุปเนื้อหาด้วยตัวเอง</h3>
<p>การสรุปเนื้อหาด้วยตัวเองจะช่วยให้เข้าใจและจำได้ดีกว่าการอ่านเฉยๆ ลองเขียนสรุปด้วยลายมือหรือสร้าง Mind Map</p>

<h3>3. ทำแบบฝึกหัดอย่างสม่ำเสมอ</h3>
<p>การทำแบบฝึกหัดอย่างสม่ำเสมอจะช่วยให้คุ้นเคยกับรูปแบบข้อสอบและเพิ่มความมั่นใจในการทำข้อสอบจริง</p>

<h3>4. ทบทวนอย่างต่อเนื่อง</h3>
<p>อย่าลืมทบทวนเนื้อหาอย่างต่อเนื่อง การทบทวนจะช่วยให้จำได้นานขึ้นและไม่ลืมเนื้อหาที่เรียนไปแล้ว</p>

<h3>5. พักผ่อนให้เพียงพอ</h3>
<p>การพักผ่อนให้เพียงพอเป็นสิ่งสำคัญเพราะจะช่วยให้สมองสดใสและพร้อมรับความรู้ใหม่ๆ</p>

<h3>6. เรียนแบบกลุ่ม</h3>
<p>การเรียนแบบกลุ่มจะช่วยให้สามารถแลกเปลี่ยนความรู้และช่วยกันแก้ปัญหาได้ดีกว่าเรียนคนเดียว</p>

<h3>7. ตั้งเป้าหมายที่ชัดเจน</h3>
<p>การตั้งเป้าหมายที่ชัดเจนจะช่วยให้มีแรงบันดาลใจและมุ่งมั่นในการเรียนมากขึ้น</p>

<h3>8. ใช้เทคนิคการจำ</h3>
<p>ลองใช้เทคนิคการจำต่างๆ เช่น การเชื่อมโยงคำ การใช้ตัวย่อ หรือการสร้างเรื่องราว</p>

<h3>9. หาแหล่งเรียนรู้เพิ่มเติม</h3>
<p>หาแหล่งเรียนรู้เพิ่มเติมนอกเหนือจากหนังสือเรียน เช่น วิดีโอ YouTube, คอร์สออนไลน์, หรือติวเตอร์</p>

<h3>10. สร้างแรงจูงใจในตัวเอง</h3>
<p>สร้างแรงจูงใจในตัวเองโดยการนึกถึงเป้าหมายที่ตั้งไว้และผลประโยชน์ที่จะได้รับจากการสอบได้คะแนนดี</p>

<p>เทคนิคเหล่านี้จะช่วยให้คุณเตรียมสอบได้อย่างมีประสิทธิภาพและทำคะแนนได้ตามที่ตั้งเป้าหมายไว้ค่ะ</p>`,
        category: 'เทคนิคการเรียน',
        icon: '📝',
        author_id: authorId,
        status: 'published',
        is_featured: true,
        display_order: 1,
        published_at: new Date()
      },
      {
        title: 'วิธีเลือกคอร์สเรียนที่เหมาะสมกับตัวเอง',
        slug: 'how-to-choose-suitable-course',
        excerpt: 'แนะนำแนวทางในการเลือกคอร์สเรียนให้เหมาะกับพื้นฐานและเป้าหมายของแต่ละคน เพื่อให้ได้ประโยชน์สูงสุดจากการเรียน',
        content: `<h2>วิธีเลือกคอร์สเรียนที่เหมาะสมกับตัวเอง</h2>
<p>การเลือกคอร์สเรียนที่เหมาะสมเป็นสิ่งสำคัญที่จะช่วยให้การเรียนมีประสิทธิภาพและบรรลุเป้าหมายที่ตั้งไว้</p>

<h3>1. ประเมินพื้นฐานของตัวเอง</h3>
<p>เริ่มต้นด้วยการประเมินพื้นฐานความรู้ของตัวเองว่าอยู่ในระดับใด เช่น ระดับเริ่มต้น ระดับกลาง หรือระดับสูง</p>

<h3>2. กำหนดเป้าหมายการเรียน</h3>
<p>กำหนดเป้าหมายที่ชัดเจนว่าต้องการเรียนเพื่ออะไร เช่น เพื่อเตรียมสอบเข้า, เพื่อเพิ่มเกรด, หรือเพื่อเรียนรู้เพิ่มเติม</p>

<h3>3. ศึกษาเนื้อหาคอร์ส</h3>
<p>ศึกษาเนื้อหาคอร์สให้ละเอียดว่าครอบคลุมอะไรบ้าง และตรงกับสิ่งที่ต้องการเรียนหรือไม่</p>

<h3>4. ตรวจสอบคุณสมบัติของอาจารย์</h3>
<p>ตรวจสอบคุณสมบัติและประสบการณ์ของอาจารย์ที่สอนคอร์สนั้นๆ เพื่อให้มั่นใจว่าจะได้รับความรู้ที่ดี</p>

<h3>5. ดูรีวิวและความคิดเห็น</h3>
<p>อ่านรีวิวและความคิดเห็นจากผู้ที่เคยเรียนคอร์สนั้นๆ เพื่อเป็นข้อมูลประกอบการตัดสินใจ</p>

<h3>6. พิจารณารูปแบบการเรียน</h3>
<p>พิจารณาว่ารูปแบบการเรียนเหมาะสมกับตัวเองหรือไม่ เช่น เรียนสด, เรียนออนไลน์, หรือเรียนแบบผสม</p>

<h3>7. ตรวจสอบค่าใช้จ่าย</h3>
<p>ตรวจสอบค่าใช้จ่ายและเปรียบเทียบกับงบประมาณที่มี เพื่อให้เลือกคอร์สที่เหมาะสมกับสถานะทางการเงิน</p>

<p>การเลือกคอร์สเรียนที่เหมาะสมจะช่วยให้การเรียนมีประสิทธิภาพและบรรลุเป้าหมายที่ตั้งไว้ค่ะ</p>`,
        category: 'เคล็ดลับ',
        icon: '📚',
        author_id: authorId,
        status: 'published',
        is_featured: true,
        display_order: 2,
        published_at: new Date()
      },
      {
        title: 'บริหารเวลาให้มีประสิทธิภาพ สำหรับนักเรียน',
        slug: 'time-management-for-students',
        excerpt: 'เคล็ดลับการแบ่งเวลาเรียนและทำกิจกรรม ให้สมดุลและไม่เครียดเกินไป เพื่อให้การเรียนและการใช้ชีวิตเป็นไปอย่างราบรื่น',
        content: `<h2>บริหารเวลาให้มีประสิทธิภาพ สำหรับนักเรียน</h2>
<p>การบริหารเวลาเป็นทักษะสำคัญที่ช่วยให้นักเรียนสามารถจัดการกับงานต่างๆ ได้อย่างมีประสิทธิภาพและไม่เครียดเกินไป</p>

<h3>1. สร้างตารางเวลาประจำวัน</h3>
<p>เริ่มต้นด้วยการสร้างตารางเวลาประจำวันที่กำหนดเวลาเรียน, ทำการบ้าน, และพักผ่อนให้ชัดเจน</p>

<h3>2. จัดลำดับความสำคัญ</h3>
<p>จัดลำดับความสำคัญของงานต่างๆ โดยทำงานที่สำคัญและเร่งด่วนก่อน</p>

<h3>3. ใช้เทคนิค Pomodoro</h3>
<p>ใช้เทคนิค Pomodoro โดยทำงาน 25 นาที แล้วพัก 5 นาที จะช่วยให้โฟกัสได้ดีขึ้น</p>

<h3>4. หลีกเลี่ยงการรบกวน</h3>
<p>หลีกเลี่ยงการรบกวนจากโซเชียลมีเดียหรือสิ่งอื่นๆ ในขณะที่กำลังเรียนหรือทำงาน</p>

<h3>5. หาเวลาพักผ่อน</h3>
<p>หาเวลาพักผ่อนและทำกิจกรรมที่ชอบ เช่น ออกกำลังกาย, ฟังเพลง, หรืออ่านหนังสือ</p>

<h3>6. อย่าลืมนอนหลับให้เพียงพอ</h3>
<p>การนอนหลับให้เพียงพอ (7-9 ชั่วโมง) จะช่วยให้สมองสดใสและพร้อมรับความรู้ใหม่ๆ</p>

<h3>7. รีวิวและปรับปรุง</h3>
<p>รีวิวและปรับปรุงตารางเวลาอย่างสม่ำเสมอ เพื่อให้เหมาะสมกับการเปลี่ยนแปลงที่เกิดขึ้น</p>

<p>การบริหารเวลาอย่างมีประสิทธิภาพจะช่วยให้การเรียนและการใช้ชีวิตเป็นไปอย่างราบรื่นและไม่เครียดเกินไปค่ะ</p>`,
        category: 'การจัดการเวลา',
        icon: '⏰',
        author_id: authorId,
        status: 'published',
        is_featured: true,
        display_order: 3,
        published_at: new Date()
      }
    ]
    
    for (const article of articles) {
      // Check if article already exists
      const [existing] = await connection.execute(
        'SELECT id FROM articles WHERE slug = ?',
        [article.slug]
      ) as any[]
      
      if (existing.length > 0) {
        console.log(`   ⏭️  Article "${article.title}" already exists, skipping...`)
        continue
      }
      
      await connection.execute(
        `INSERT INTO articles (
          title, slug, excerpt, content, category, icon, featured_image_url,
          author_id, status, is_featured, display_order, published_at
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
        [
          article.title,
          article.slug,
          article.excerpt,
          article.content,
          article.category,
          article.icon,
          article.featured_image_url || null,
          article.author_id,
          article.status,
          article.is_featured,
          article.display_order,
          article.published_at
        ]
      )
      
      console.log(`   ✅ Created article: ${article.title}`)
    }
    
    console.log('')
    
    // ============================================
    // TESTIMONIALS
    // ============================================
    console.log('⭐ Creating sample testimonials...')
    
    const testimonials: Testimonial[] = [
      {
        name: 'คุณสมใจ',
        role: 'ผู้ปกครองของน้องน้ำ',
        comment: 'ลูกเรียนแล้วเข้าใจมากขึ้น ผลการเรียนดีขึ้นมาก คุณครูสอนดีมาก มีเทคนิคที่ทำให้เด็กเข้าใจง่าย ลูกชอบเรียนที่นี่มาก',
        rating: 5,
        status: 'approved',
        display_order: 1
      },
      {
        name: 'คุณวราภรณ์',
        role: 'ผู้ปกครองของน้องเจมส์',
        comment: 'ระบบเรียนออนไลน์สะดวกมาก ลูกสามารถเรียนได้ทุกที่ และเรียนซ้ำได้ตลอด ช่วยประหยัดเวลาเดินทาง และลูกสามารถเรียนได้ทุกเวลา',
        rating: 5,
        status: 'approved',
        display_order: 2
      },
      {
        name: 'น้องน้ำ',
        role: 'นักเรียนชั้น ม.3',
        comment: 'คุณครูสอนสนุก เข้าใจง่าย และมีแบบฝึกหัดให้ทำเยอะมาก เรียนแล้วไม่เครียด และได้ความรู้เต็มที่ ชอบวิธีการสอนของอาจารย์มากๆ',
        rating: 5,
        status: 'approved',
        display_order: 3
      }
    ]
    
    for (const testimonial of testimonials) {
      await connection.execute(
        `INSERT INTO testimonials (
          name, role, comment, rating, avatar_url, status, display_order
        ) VALUES (?, ?, ?, ?, ?, ?, ?)`,
        [
          testimonial.name,
          testimonial.role,
          testimonial.comment,
          testimonial.rating,
          testimonial.avatar_url || null,
          testimonial.status,
          testimonial.display_order
        ]
      )
      
      console.log(`   ✅ Created testimonial: ${testimonial.name}`)
    }
    
    console.log('')
    console.log('✅ Content management seeding completed!')
    console.log('')
    
  } catch (error: any) {
    console.error('❌ Error seeding content management:', error.message)
    console.error(error)
    process.exit(1)
  } finally {
    await connection.end()
  }
}

// Load .env if exists
try {
  const envPath = join(process.cwd(), '.env')
  const envContent = readFileSync(envPath, 'utf-8')
  envContent.split('\n').forEach(line => {
    const [key, ...values] = line.split('=')
    if (key && values.length) {
      const value = values.join('=').trim()
      if (!process.env[key.trim()]) {
        process.env[key.trim()] = value
      }
    }
  })
} catch (error) {
  // .env file not found, use defaults
}

seedContentManagement()

